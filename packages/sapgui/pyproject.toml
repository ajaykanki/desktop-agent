[project]
name = "sapgui"
description = "Library to interact with the SAP Logon 770 Client using SAP GUI Scripting API."
version = "0.1.0"
requires-python = ">=3.11.13"
dependencies = [
    "pywinauto>=0.6.9",
]

[build-system]
requires = ["uv_build>=0.8.16,<0.9.0"]
build-backend = "uv_build"
