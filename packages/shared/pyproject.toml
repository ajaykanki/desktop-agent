[project]
name = "shared"
version = "0.1.0"
requires-python = ">=3.11.13"
dependencies = [
    "keyring>=25.6.0",
]

[project.scripts]
shared = "shared:main"

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"
