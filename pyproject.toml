[project]
name = "desktop-agent"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.11.13"

[tool.uv.workspace]
members = [
    "packages/sapgui",
    "packages/api",
    "packages/worker",
    "packages/shared",
]

[tool.uv.sources]
sapgui = { workspace = true }
api = { workspace = true }
worker = { workspace = true }
shared = { workspace = true }
